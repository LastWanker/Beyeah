# 第一阶段可执行落地清单（M1）

更新时间：2026-03-07

## 目标
- 打通前端交易主链路：商品浏览 -> 购物车 -> 下单 -> 订单查询。
- 统一前后端调用契约到 `/api/v1/*`。
- 修复前端 4 个关键问题：删车传参、登出清车、结算金额响应式、加购假成功。

## 执行清单

### 1. 后端 API 契约（`/api/v1`）
- [x] 新增 `GET /api/v1/home/new`
- [x] 新增 `GET /api/v1/home/hot`
- [x] 新增 `GET /api/v1/goods`
- [x] 新增 `GET /api/v1/goods/{goodsId}`
- [x] 新增 `GET /api/v1/cart`
- [x] 新增 `POST /api/v1/cart/items`
- [x] 新增 `PUT /api/v1/cart/items/{cartItemId}`
- [x] 新增 `DELETE /api/v1/cart/items/{cartItemId}`
- [x] 新增 `POST /api/v1/orders`
- [x] 新增 `GET /api/v1/orders`
- [x] 新增 `GET /api/v1/orders/{orderNo}`
- [x] 新增 `POST /api/v1/auth/login`
- [x] 新增 `POST /api/v1/auth/register`
- [x] 新增 `GET /api/v1/users/me`
- [x] 新增 `PUT /api/v1/users/me`

### 2. 后端关键修复
- [x] 修复 `MallUserMapper` 的 `updateByPrimaryKeySelective` 误写为 `static`。
- [x] 重写 `beyeahUserServiceImpl`，移除业务路径中 `DatabaseUtil/JDBC` 依赖，统一 MyBatis。
- [x] 清理 `MallUser` 实体中的 JDBC 残留方法与连接状态。
- [x] 清理 legacy 交易残留：删除旧 `controller/mall/*` 与旧 `CartService` 体系文件。
- [x] 增加 `ApiUserResolver`，统一解析 `userId`（query/header）。

### 3. 前端契约切换
- [x] `frontend/src/service/home.js` 切换到 `/api/v1/home/*`
- [x] `frontend/src/service/goods.js` 切换到 `/api/v1/goods`
- [x] `frontend/src/service/detail.js` 切换到 `/api/v1/goods/{id}`
- [x] `frontend/src/service/cart.js` 切换到 `/api/v1/cart/*`
- [x] `frontend/src/service/order.js` 切换到 `/api/v1/orders/*`
- [x] `frontend/src/service/login_reg.js` 切换到 `/api/v1/auth/*`
- [x] `frontend/src/service/userInfo.js` 切换到 `/api/v1/users/me`
- [x] `frontend/src/utils/axios.js` 补统一错误映射。

### 4. 前端关键问题修复
- [x] 删车传参：按 `cartItemId` 删除，修复 `UserCart.vue` -> `cartStore.delCart(...)`。
- [x] 登出不清车：`user.clearInfo()` 真实调用 `cartStore.cleanCart()`。
- [x] 结算总价响应式：`Checkout.vue` 改为 `computed`。
- [x] 加购假成功：`ProductDetail.vue` 按接口结果提示成功/失败。

### 5. 数据库迁移
- [x] 新增 `database/migrations/V20260307_01_contract_unify.sql`。
- [x] 包含：`order_no` 唯一索引、购物车/订单/商品组合索引、`login_name` 扩容到 32。

### 6. 编译与构建验收
- [x] 后端编译通过：`backend\mvnw.cmd -DskipTests compile`
- [x] 前端构建通过：`frontend\npm run build`

## 阶段一状态
- 阶段一核心目标：已完成。
- 可选增强（未做）：Postman/HTTPie 自动化回归脚本可在后续补充。
