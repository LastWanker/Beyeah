# 第三阶段可执行落地清单

## 批次 A（P0）：防超卖 + 幂等 + 订单超时关闭
### 已完成
- [x] 新增迁移脚本 `V20260308_01_batch_a_order_hardening.sql`
- [x] 订单表新增 `idempotency_key` 字段
- [x] 新增唯一索引 `uk_order_user_idempotency(user_id, idempotency_key)`
- [x] 下单接口支持幂等键（请求头与请求体）
- [x] 同幂等键重复请求返回同一订单号
- [x] 下单流程事务重构：扣库存 -> 写订单 -> 写订单项 -> 删购物车
- [x] 库存扣减校验改为“全量成功才通过”
- [x] 修复回补库存逻辑
- [x] 新增超时未支付订单自动关闭任务
- [x] 关闭超时订单后自动回补库存
- [x] 前端结算提交带幂等键并防重复点击

### 待补强（建议）
- [ ] 在 CI 环境执行“并发 100 下单”压力回归
- [ ] 增加定时任务监控指标（扫描量、关闭量、失败量）
- [ ] 增加幂等冲突日志聚合（按 userId/idempotencyKey）

## 批次 B（P1）：地址簿
### 已完成
- [x] 新增迁移脚本 `V20260308_02_batch_b_address_book.sql`
- [x] 新增地址簿表 `beyeah_mall_user_address`
- [x] 新增地址接口：
  - `GET /api/v1/addresses`
  - `POST /api/v1/addresses`
  - `PUT /api/v1/addresses/{id}`
  - `DELETE /api/v1/addresses/{id}`
  - `PUT /api/v1/addresses/{id}/default`
- [x] 默认地址唯一约束（服务层保证，同一用户仅一个 `is_default=1`）
- [x] 下单支持 `addressId`，并优先使用地址簿快照写入订单
- [x] 订单快照补全 `user_name/user_phone/user_address`
- [x] Checkout 接入地址簿选择并按 `addressId` 下单
- [x] 个人中心新增地址簿管理（增删改、设默认）
- [x] 后端集成测试新增地址簿 + 快照不变更用例
- [x] 前端 E2E 用例适配地址簿链路

## 批次 C（P1）：退款/售后
### 已完成
- [x] 新增迁移脚本 `V20260308_03_batch_c_after_sale_refund.sql`
- [x] 订单表新增退款聚合字段：`refund_status`、`refund_time`
- [x] 新增售后表 `beyeah_mall_after_sale`
- [x] 新增退款流水表 `beyeah_mall_refund`
- [x] 新增售后状态枚举与退款状态枚举
  - `AfterSaleStatusEnum`
  - `RefundStatusEnum`
  - `OrderRefundStatusEnum`
- [x] 新增用户侧售后接口：
  - `POST /api/v1/after-sales`
  - `GET /api/v1/after-sales`
  - `GET /api/v1/after-sales/{afterSaleNo}`
- [x] 新增管理侧审核接口：
  - `GET /api/v1/admin/after-sales`
  - `PUT /api/v1/admin/after-sales/{id}/approve`
  - `PUT /api/v1/admin/after-sales/{id}/reject`
- [x] 审核通过闭环打通（申请 -> 审核通过 -> 退款成功 -> 订单退款状态回写）
- [x] 重复审核幂等处理（重复 approve 不重复创建退款）
- [x] 鉴权拦截已纳入售后路由：
  - `/api/v1/after-sales/**`
  - `/api/v1/admin/after-sales/**`
- [x] 前端接入售后能力：
  - 新增 `frontend/src/service/afterSale.js`
  - `UserInfo` 订单详情页支持“申请售后 + 售后记录展示”
- [x] 后端集成测试新增售后通过/拒绝主链路用例（`ApiIntegrationTest`）

### 待补强（建议）
- [x] 管理侧补充 RBAC（详见 `docs/可执行落地清单/04-管理员专项-可执行落地清单.md`）
- [ ] 增加支付渠道真实回调联调（当前为 mock 退款通道）
- [ ] 增加前端 E2E 覆盖售后链路（申请、列表、状态流转）
- [ ] 增加售后/退款关键指标与告警（申请量、审核量、成功率、失败率）

## 批次 D（P2）：增强能力包
- [ ] 未开始
