# 管理员专项可执行落地清单（激进单批版）

更新时间：2026-03-08  
关联方案：`docs/修复与优化方案/04-管理员专项分析.md`

## 执行结果总览（本次一次性执行）
- 已完成：数据库迁移/回滚、后端 RBAC 链路、管理 API 权限化、操作审计、前端管理员登录与控制台、前端 E2E smoke。  
- 部分未完成：管理员后端集成测试“实跑通过”在当前环境受限（本机无 Docker，Testcontainers 用例被跳过）；线上灰度验收未执行。  
- 执行中调整：将种子中的中文名称改为英文，避免脚本编码导致 SQL 引号断裂。

---

## A. 数据库（必须）
### A1. 迁移脚本
- [x] 新增 `database/migrations/V20260308_04_admin_rbac.sql`
- [x] 新增表：
  - `beyeah_admin_role`
  - `beyeah_admin_permission`
  - `beyeah_admin_user_role`
  - `beyeah_admin_role_permission`
  - `beyeah_admin_operation_log`

### A2. 种子数据
- [x] 初始化角色：`SUPER_ADMIN / MERCHANDISER / WAREHOUSE / CUSTOMER_SERVICE / RISK_OPERATOR / AUDITOR`
- [x] 初始化权限（最小集）：
  - `goods:read`, `goods:write`
  - `order:read`, `order:fulfill`, `order:close`
  - `user:read`, `user:lock`
  - `after_sale:read`, `after_sale:approve`
  - `admin:dashboard:read`
- [x] 回填已有 `beyeah_mall_admin_user` 到 `SUPER_ADMIN`

### A3. 回滚要求
- [x] 新增回滚脚本：`database/migrations/V20260308_04_admin_rbac_rollback.sql`
- [x] 回滚脚本仅操作 RBAC/审计表，不影响 C 端交易链路

---

## B. 后端（必须）
### B1. 管理员认证链路
- [x] `POST /api/v1/admin/auth/login`（与 C 端 token 分离）
- [x] `AdminAuthInterceptor` 拦截 `/api/v1/admin/**`
- [x] `AdminAuthContext`（`adminId`、`roles`、`permissions`）

### B2. RBAC 鉴权
- [x] `@RequirePermission("resource:action")`
- [x] 权限拦截器统一 403
- [x] 管理 API 全量权限注解（无裸接口）

### B3. 管理 API 串联改造
- [x] 商品管理 API：列表/新增/更新/上下架权限化
- [x] 订单管理 API：列表/配货/出库/关闭权限化
- [x] 用户管理 API：列表/封禁/解封权限化
- [x] 售后审核 API：列表/通过/拒绝权限化

### B4. 操作审计
- [x] `AdminOperationLogService` 落地
- [x] 写操作审计覆盖：商品、订单、用户、售后四类
- [x] 字段覆盖：`operator_admin_id, action, resource_type, resource_id, request_json, response_code, trace_id, ip, create_time`

---

## C. 前端（必须）
### C1. 管理员单页控制台
- [x] 新增路由 `/admin-console`
- [x] 新增 `frontend/src/views/AdminConsole.vue`
- [x] 单页 4 模块：
  - 商品管理（查询 + 上下架）
  - 订单履约（配货 + 出库 + 关闭）
  - 用户治理（封禁/解封）
  - 售后审核（通过/拒绝）

### C2. 管理 API 接入
- [x] `frontend/src/service/adminAuth.js`
- [x] `frontend/src/service/adminGoods.js`
- [x] `frontend/src/service/adminOrders.js`
- [x] `frontend/src/service/adminUsers.js`
- [x] `frontend/src/service/adminAfterSales.js`

### C3. 前端权限与会话
- [x] 管理员 token 与 C 端 token 分离存储
- [x] 管理员路由守卫（未登录跳转管理员登录）
- [x] 401/403 统一处理（403 明确提示权限不足）
- [x] 修复顶部“手机/美妆”下拉菜单 hover 缝隙，保证可达

---

## D. 测试与发布（必须）
### D1. 后端集成测试
- [x] 新增管理员登录用例
- [x] 新增 401/403 权限拒绝用例
- [x] 新增 4 类管理写操作成功用例
- [x] 新增审计日志落库断言
- [ ] 当前环境实跑通过（依赖 Docker；本机无 Docker，Testcontainers 被跳过）

### D2. 前端 E2E
- [x] 新增管理员控制台 smoke 用例：
  - 登录 -> 进入控制台 -> 执行 1 次商品上下架
  - 执行 1 次售后审核通过
- [x] 本地执行通过（Playwright）

### D3. 发布门槛（硬性）
- [x] `backend` 编译通过
- [x] `frontend` 构建通过
- [x] 管理员 E2E smoke 通过
- [ ] 管理员相关后端集成测试在 CI/Docker 环境通过
- [ ] 线上灰度环境完成功能验收

---

## E. Done Definition 对照
1. `/api/v1/admin/**`：未登录 401，无权限 403。  
状态：已通过用例覆盖（本地测试代码完成，待 CI Docker 实跑确认）。  
2. 同接口不同角色返回不同。  
状态：已覆盖（`goods-admin` 对售后审核返回 403）。  
3. 管理员单页闭环四类操作。  
状态：已完成。  
4. 写操作审计可追踪。  
状态：已完成并有断言。  
5. 不破坏 C 端交易链路。  
状态：前端 E2E（checkout）通过，后端编译通过。

## F. 建议后续动作
1. 在 CI 机器安装 Docker，执行 `backend` Testcontainers 集成测试全量通过。  
2. 灰度环境先仅开放 `SUPER_ADMIN`，观测无异常后逐步开放其他角色。  
3. 增加管理员接口监控指标（401/403 比例、写操作失败率、审计日志写入失败率）。
